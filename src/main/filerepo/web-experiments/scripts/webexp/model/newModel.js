
define(['./BaseModel'], function (Base) {

    var newModel = new Base('This is the data for Page 1')

    // Extending BaseModel for new page/app

    // newModel.trials = []

    // Passing on newModel

    return newModel;

});

